/**
 * Placeholder for a dedicated client session store (e.g. Zustand).
 * Auth state is implemented in `context/AuthContext.tsx` using Context API + localStorage.
 */
export { AUTH_STORAGE_KEYS } from './session'
